# Large dataset metadata — fetch from the Drive link in reference.md
gdrive_files/

# Ego-Exo4D metadata + linked raw data (large; fetch separately)
egoexo4D/

# Symlinks to training datasets on the external Seagate disk (never commit)
cooking_train
diverse50_train

# Model weights / checkpoints (never commit)
checkpoints/
*.safetensors
*.ckpt
*.pth

# Local build/run logs
*.log

# local tooling
.gdown-venv/

# 24GB repro: logs + generated outputs (keep code/docs only)
*.log
wan_smoketest/outputs/
**/__pycache__/

# Blackwell local scratch: scripts, runs, render outputs (never pushed)
local/
egoX_mix_train
